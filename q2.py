sentence = input(str("enter any sentence"))
def countalpha(sentence)
    alpha_freq ={}
    alpha = char.upper() for char in sentence
    for x in alpha
        if x in alpha_freq:
            alpha_freq[w] +=1
        else:
            alpha_freq[w]=1
    return alpha_freq

def countalpha(sentence)
    word_freq ={}
    word = char.upper() for char in sentence
    for x in word
        if x in word_freq:
            word_freq[w] +=1
        else:
            word_freq[w]=1
  return word_freq



    
        
