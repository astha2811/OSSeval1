sentence = input(str("enter any sentence"))
def countalpha(sentence)
    alpha_freq ={}
    alpha = char.upper() for char in sentence
    for x in alpha
        if x in alpha_freq:
            alpha_freq[w] +=1
        else:
            alpha_freq[w]=1
    return alpha_freq

def countalpha(sentence)
    alpha_freq ={}
    alpha = char.lower() for char in sentence
    for x in alpha
        if x in alpha_freq:
            alpha_freq[w] +=1
        else:
            alpha_freq[w]=1
    return alpha_freq
